# Trigger the workflow on push (only for the master branch) or pull request
on:
  pull_request:
  push:
    branches:
      - master

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci
      - run: npm run lint:js
      - run: npm run lint:style
